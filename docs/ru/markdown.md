# Listing Generator · Руководство по работе с Markdown

Этот документ описывает **все возможности Markdown-адаптера** в Listing Generator (LG): нормализацию заголовков, вырезание шумных разделов (по заголовкам и путям), работу с front matter и практики сборки «плотных» промтов.

---

## Зачем это нужно

LLM-модели заметно лучше отвечают, когда:
- видят **структурированный Markdown** (корректные уровни заголовков),
- не получают **шум** (установка/лицензии/устаревшее),
- контекст **помещается в окно** модели.

Markdown-адаптер помогает:
1) Нормализовать заголовки (снять одинокий H1, сдвинуть уровни).
2) Системно **вырезать целые разделы** по заголовкам (с поддеревом).
3) Снести **YAML front matter** в начале.
4) Вставить **плейсхолдеры** на месте удалённого (по желанию).

> ⚠️ Вырезание по заголовкам происходит **вне fenced-кода**. Содержимое ```кодовых блоков``` никогда не анализируется как Markdown-структура.

---

## Коротко: минимальная конфигурация

### Drop режим — удаление указанного контента

```yaml
# lg-cfg/config.yaml (фрагмент)
docs:
  extensions: [".md"]
  markdown:
    # 1) Нормализация заголовков
    max_heading_level: 2   # самый верхний уровень в результирующем документе → H2

    # 2) Что вырезать (и почему)
    drop:
      sections:
        - match: { kind: text, pattern: "Installation" }   # удалить раздел «Installation» целиком
          level_at_most: 3
          reason: "user-facing install guide"
        - match: { kind: slug, pattern: "^cli-options$" }  # по slug, стабильно к пунктуации/регистру
        - match: { kind: regex, pattern: "^(Legacy|Deprecated)", flags: "i" }
        - path: ["FAQ", "User"]                            # ветка по родителям → удалить поддерево

      frontmatter: true  # удалить YAML front matter (--- ... ---) в начале

    # 3) Как обозначить пропуски
    placeholder:
      mode: summary      # none|summary
      template: "> *(Опущен раздел «{title}»; −{lines} строк)*"
```

### Keep режим — сохранение только указанного контента

```yaml
# lg-cfg/config.yaml (фрагмент) 
docs:
  extensions: [".md"]
  markdown:
    # 1) Нормализация заголовков
    max_heading_level: 2   # самый верхний уровень в результирующем документе → H2

    # 2) Что оставить (инверсия drop-логики)
    keep:
      sections:
        - match: { kind: text, pattern: "Usage" }          # оставить только раздел «Usage»
        - match: { kind: slug, pattern: "api-reference" }  # и справочник API
        - match: { kind: regex, pattern: "^Important", flags: "i" }  # и важные разделы

      frontmatter: true  # сохранить YAML front matter (--- ... ---)

    # 3) В keep режиме плейсхолдеры не используются — удалённое просто исчезает
```

> ⚠️ **Важно**: режимы `drop` и `keep` взаимоисключающие — нельзя использовать одновременно.

---

## Как это работает

### Порядок операций (pipeline)

1. **Парсинг** Markdown:

   * обнаружение fenced-блоков (\`\`\`/\~\~\~) и их границ;
   * заголовки **ATX** (`#..######`) и **Setext** (подчёркивания `===` / `---`);
   * дерево разделов: `title`, `level`, граница поддерева.

2. **Формирование интервалов удаления**:

   * `drop.sections`: целые поддеревья по правилам `text|slug|regex|path`;
   * `frontmatter`: YAML-шапка в начале (“--- … ---”).

3. **Слияние пересечений** → единый набор интервалов.

4. **Трансформация**:

   * вырезаем интервалы;
   * по политике `placeholder` вставляем краткие заглушки с данными (название, кол-во строк/байт).

5. **Нормализация заголовков**:

   * если `strip_h1=true` → снимаем верхний **H1** (ATX или Setext);
   * затем сдвинуть уровни так, чтобы верхний стал `max_heading_level`

> 💡 Нормализация выполняется **после** удаления, чтобы уровни были корректны в «сшитом» промте.

---

## Справочник по конфигурации

### Ключ `markdown`

| Поле                | Тип       | По умолчанию | Описание                                                                                           |
|---------------------|-----------|--------------|----------------------------------------------------------------------------------------------------|
| `max_heading_level` | int\|null | null         | Если задано — сдвигает уровни заголовков (вне fenced), чтобы минимальный стал равен этому значению. |
| `strip_h1`   | bool      | false        | Удаляет верхний H1 (ATX или setext).                                                               |
| `drop`              | object    | —            | Настройки вырезания (взаимоисключающе с `keep`).                                                   |
| `keep`              | object    | —            | Настройки сохранения контента (взаимоисключающе с `drop`).                                         |
| `placeholder`       | object    | —            | Политика плейсхолдеров для удалённых блоков (только для `drop` режима).                            |

---

### `drop.sections[]` — удаление по заголовкам

Каждый элемент — **одно правило**, которое выбирает 0..N разделов (с поддеревьями):

```yaml
drop:
  sections:
    - match: { kind: text|slug|regex, pattern: "<строка>" , flags: "ims" }  # один из match-режимов
      path:  ["Parent A", "Parent B"]     # (опц.) ограничить контекстом родителей
      level_at_least: 1                   # (опц.) фильтр по уровню заголовка
      level_at_most:  3
      level_exact:    2
      reason: "legacy docs"               # (опц.) попадёт в метаданные отчёта
      placeholder: "> *(Раздел «{title}» опущен)*"  # (опц.) переопределить глобальный шаблон
```

* `kind: text` — точное сравнение `title` (без форматирования).
* `kind: slug` — сравнение **slug** (GitHub-style): в нижнем регистре, пробелы→дефисы, убрана пунктуация.
  Примеры:
  `“CLI Options & Flags” → "cli-options-flags"`,
  `“FAQ: User” → "faq-user"`.
* `kind: regex` — регулярное выражение по `title`. `flags` — строка из `i m s` (ignorecase, multiline, dotall).
* `path` — список **точных** заголовков-предков. Полезно, когда одинаковые названия встречаются в разных ветках.

> Пример: удалить только «Deprecated» **внутри** «API / v1»:
>
> ```yaml
> - path:  ["API", "v1"]
>   match: { kind: text, pattern: "Deprecated" }
> ```

---

### `keep.sections[]` — сохранение только указанных разделов

**Keep режим** работает **инверсивно** к drop режиму: указывает какие разделы **оставить**, всё остальное удаляется.

```yaml
keep:
  sections:
    - match: { kind: text|slug|regex, pattern: "<строка>", flags: "ims" }  # один из match-режимов  
      path:  ["Parent A", "Parent B"]     # (опц.) ограничить контекстом родителей
      level_at_least: 1                   # (опц.) фильтр по уровню заголовка
      level_at_most:  3
      level_exact:    2
      reason: "important content"         # (опц.) попадёт в метаданные отчёта
      # placeholder НЕ поддерживается — удалённое просто исчезает
```

Все селекторы работают точно так же, как в `drop` режиме:

* `kind: text` — точное сравнение заголовка.
* `kind: slug` — сравнение по GitHub slug.  
* `kind: regex` — регулярное выражение.
* `path` — ограничение по цепочке родителей.

> **Важные отличия keep режима:**
> 
> * **Плейсхолдеры не вставляются** — удалённый контент просто исчезает.
> * **Сохраняются поддеревья** — при сохранении раздела H2 сохраняются все его подразделы H3, H4 и т.д.
> * **Пустой список** `sections: []` удалит весь контент (кроме frontmatter если `frontmatter: true`).

> Пример: оставить только «Usage» и «API Reference»:
>
> ```yaml  
> keep:
>   sections:
>     - match: { kind: text, pattern: "Usage" }
>     - match: { kind: slug, pattern: "api-reference" }
> ```

### `drop.frontmatter` — удаление YAML front matter

```yaml
drop:
  frontmatter: true
```

Если документ начинается строкой `---`, всё до следующей строки `---` вне fenced-кода будет удалено.

> Поддерживается именно YAML-стиль `---`. TOML-вариант `+++` не обрабатывается.

---

### `placeholder` — как обозначать «пропуски»

```yaml
placeholder:
  mode: none | summary
  template: "> *(Опущен раздел «{title}»; −{lines} строк)*"
```

* `mode: none` — просто вырезать.
* `mode: summary` — вставить строку по `template`.
* Доступные переменные: `{title}`, `{level}`, `{lines}`, `{bytes}`.
* Пер-правило можно переопределить шаблон в самом правиле `sections[].placeholder`.

---

## Нормализация заголовков

* **Снятие H1**: если в конфигурации секции установлен `strip_h1: true` и первая строка — `# ...` (или Setext-заголовок), заголовок удаляется, чтобы не плодить «вторых H1» в комбинированных контекстах.
* **Сдвиг уровней**: если указан `max_heading_level`, уровни заголовков **вне fenced-кода** сдвигаются так, чтобы самый верхний стал именно этим уровнем (обычно `2`).

Это важно при **сборке через шаблоны** (`lg-cfg/contexts/*.tpl.md`): когда вы вставляете несколько секций, заголовки сохраняют корректную иерархию, и модель «видит» структуру.

---

## Рецепты

### Удалить установку, CLI и лицензию

```yaml
docs:
  extensions: [".md"]
  markdown:
    max_heading_level: 2
    drop:
      sections:
        - match: { kind: text, pattern: "Installation" }
        - match: { kind: slug, pattern: "^cli" }          # CLI, CLI Options, CLI Usage…
        - match: { kind: regex, pattern: "^License$", flags: "i" }
    placeholder:
      mode: summary
      template: "> *(Опущен «{title}»)*"
```

### Ровнять уровни заголовков при сшивке секций

```yaml
guides:
  extensions: [".md"]
  markdown:
    max_heading_level: 2   # В итоговом документе верхние заголовки → H2
```

Шаблон:

```markdown
# Руководство

${tpl:intro}      <!-- вложенный шаблон -->
${guides}         <!-- секция Markdown -->
${tpl:appendix}
```

### Адресные правила через `targets`

Удаляем «License» **только** в `docs/**.md`, не трогая `LICENSE.md`:

```yaml
docs:
  extensions: [".md"]
  targets:
    - match: "/docs/**.md"
      markdown:
        drop:
          sections:
            - match: { kind: text, pattern: "License" }
```

### Сравнение Drop vs Keep режимов

**Drop режим** лучше когда нужно **убрать отдельные помехи**:
- Удалить установку из документации разработчика
- Исключить deprecated API из справочника
- Убрать user-facing контент из technical docs

**Keep режим** эффективнее когда нужно **сильно сократить** контент:
- Оставить только Usage из длинного README  
- Извлечь только API Reference из комплексной документации
- Сохранить только ключевые разделы из большой инструкции

---

## Диагностика и отчёты

Команда:

```bash
lg report ctx:my-context --model gpt-4o > report.json
```

В `files[].meta` для Markdown-файлов появятся поля:

* `md.mode` — используемый режим: `"drop"` или `"keep"`.
* `md.removed.sections` — количество удалённых/обработанных разделов.
* `md.removed.frontmatter` — `true/false`.
* `md.placeholders` — сколько вставлено плейсхолдеров (только в drop режиме).

Это помогает понять, что «ест» токены и какие правила реально сработали.

---

## Ограничения и поведение по умолчанию

* **Fenced-код**: заголовки внутри \`\`\`/\~\~\~ игнорируются как структура.
* **Blockquote-заголовки** (`> # Заголовок`) не считаются структурными заголовками для удаления по секциям.
* **Setext** поддерживается для уровней H1 (`===`) и H2 (`---`) — как в CommonMark.
* **Front matter** распознаётся только YAML-стиля `---`.
* **Регулярные выражения** — стандартные Python-регэкспы (не PCRE). Для большинства задач хватает `i` (ignorecase).
* Если `max_heading_level` не задан — уровни не сдвигаются.
* Снятие H1 происходит только если явно установлен флаг `strip_h1: true` в конфигурации секции.

