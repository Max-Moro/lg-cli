
val simple = { "hello" }

val complex = { a: Int, b: Int ->
    // … lambda body omitted (3 lines)
}

val multiline: (List<User>) -> List<String> = { users ->
    // … lambda body omitted (4 lines)
}
