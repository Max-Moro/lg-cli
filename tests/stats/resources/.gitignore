# Ignore downloaded tokenization models
# These files are large and should be downloaded locally

# HuggingFace tokenizers
tokenizers/*/tokenizer.json
tokenizers/*/*.json

# SentencePiece models
sentencepiece/*/*.model
sentencepiece/*/*.spm

# But keep manifest and README
!models_manifest.json
!README.md

# Also ignore HuggingFace temporary files
**/.cache/
**/downloads/
