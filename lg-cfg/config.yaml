schema_version: 6
docs-intro:
  extensions: [".md"]
  filters:
    mode: allow
    allow:
      - "/README.md"

common-src:
  extensions: [".py", ".toml"]
  filters:
    mode: allow
    allow:
      - "/pyproject.toml"
      - "/lg/"
      - "/tests/test_adapters_registry.py"
      - "/tests/test_cli_minimum.py"
      - "/tests/conftest.py"

tests-src:
  extensions: [".py"]
  filters:
    mode: allow
    allow:
      - "/tests/"
