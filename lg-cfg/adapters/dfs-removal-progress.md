# Прогресс удаления DFS из literals optimization

**Дата начала**: 2025-12-13
**Статус**: В процессе отладки
**Текущая ветка**: `literals-v2`

---

## Цель рефакторинга

Упростить архитектуру literals optimization путем удаления ручной рекурсии (DFS) из компонентов и перенесения всей логики композиции на pipeline через механизм `add_replacement_composing_nested`.

---

## Выполненные изменения

### 1. Удаление ручной рекурсии из BlockInitProcessorBase ✅

**Коммит**: `7c4e187` - "refactor(literals): remove manual recursion, introduce inside-out processing order"

**Изменения**:
- Удалено `_optimize_statement_recursive` (~150 строк)
- Убраны параметры `all_profiles` и `process_literal_callback`
- JavaDoubleBraceProcessor и RustLetGroupProcessor упрощены - используют `doc.get_node_text()` напрямую

**Результат**: -158 строк кода, все 100 тестов проходят

### 2. Введение универсальной inside-out сортировки ✅

**Включено в коммит**: `7c4e187`

**Изменения в pipeline**:
- Убрана фильтрация top-level vs nested (все узлы обрабатываются)
- Добавлена сортировка по глубине: `sort(key=lambda n: (-node_depth(n), n.start_byte))`
- Deepest-first порядок обеспечивает правильную композицию через `add_replacement_composing_nested`

**Результат**: Универсальная основа для полной комбинаторики всех профилей

### 3. Удаление DFS из StandardCollectionsProcessor ✅

**Коммит**: `[будет добавлен]`

**Изменения**:
- `BudgetSelector`: удален `DFSSelection` класс, переименован `select_dfs()` → `select()`
- `CollectionFormatter`: удалены `format_dfs()`, `_reconstruct_element_with_nested()`, `_should_use_inline_nested()`
- `StandardCollectionsProcessor`: использует упрощенные методы

**Результат**: -300+ строк DFS логики удалено

### 4. Создание CppInitializerListProcessor ✅

**Коммит**: `51a98c0`

**Проблема**: C++ AST создает `initializer_list` узлы для всех:
- Полноценных коллекций: `{{"x", 1}, {"y", 2}}`
- Map entries (пар): `{"key", {...}}`
- Элементов: `{"x", 1}`

При inside-out обработке entries обрабатывались как коллекции, что ломало структуру.

**Решение**: Создан специализированный процессор `CppInitializerListProcessor`:
- Наследуется от `StandardCollectionsProcessor`
- В `can_handle()` принимает все `initializer_list`
- В `process()` фильтрует entries (возвращает `None`)
- Делегирует обработку родителю

**Эвристика определения entry**:
- Parent тоже `initializer_list`
- Первый значимый child - scalar (не `initializer_list`)

**Изменения**:
- Создан `lg/adapters/optimizations/literals/components/cpp_initializer_list.py`
- Зарегистрирован в `LiteralPipeline` перед `StandardCollectionsProcessor`
- Убран хак из pipeline фильтрации

**Архитектурный принцип**: Language-specific логика в компонентах, не в универсальном pipeline

**Результат**: Структура C++ nested maps сохраняется корректно

### 5. Унифицированная inside-out сортировка для всех профилей ✅

**Коммит**: `bb81bef`

**Проблема**: Inside-out сортировка работала ВНУТРИ одного профиля, но НЕ МЕЖДУ разными профилями:
```python
for profile in self.descriptor.profiles:
    self._process_profile(context, profile, max_tokens)  # Каждый профиль отдельно
```

Пример Java:
- `Map.ofEntries` (depth=13) обрабатывался ПЕРВЫМ
- `List.of` (depth=17, ГЛУБЖЕ!) обрабатывался ВТОРЫМ

Edit для `List.of` создавался, но потом перезаписывался родительским `Map.ofEntries`.

**Решение**: Собирать ВСЕ nodes от ВСЕХ profiles и сортировать их ВМЕСТЕ по depth:
```python
# Собрать все (node, profile) пары
all_node_profile_pairs = []
for profile in self.descriptor.profiles:
    nodes = context.doc.query_nodes(profile.query, "lit")
    # ... фильтрация ...
    all_node_profile_pairs.append((node, profile))

# Сортировка ПО DEPTH независимо от profile type
all_node_profile_pairs.sort(key=lambda pair: (-node_depth(pair[0]), pair[0].start_byte))

# Обработка в едином проходе
for node, profile in all_node_profile_pairs:
    self._process_node(context, node, profile, max_tokens)
```

**Изменения**:
- Рефакторинг `apply()`: сбор всех nodes перед обработкой
- Переименование `_process_profile()` → `_process_node()`
- Единая сортировка по depth для всех profile types

**Архитектурный принцип**: Depth важнее порядка профилей в дескрипторе

**Результат**:
- ✅ Вложенные литералы разных типов обрабатываются корректно
- ✅ Универсальное решение для всех языков
- ✅ Java `List.of` внутри `Map.ofEntries` оптимизируется правильно

---

## Текущее состояние тестов

**Прогон**: `./scripts/test_adapters.sh literals all`
**Результат**: 16 failed, 84 passed

### Падающие тесты (паттерн)

Все языки (C++, Go, Java, JS, Kotlin, Python, Scala, TS):
- `test_basic_literal_trimming_budget_10`
- `test_basic_literal_trimming_budget_20`

---

## Анализ проблем

### Проблема №1: Испорченная структура (C++) ✅ РЕШЕНА

**Было (после удаления DFS)**:
```cpp
std::map<std::string, std::map<std::string, int>> largeConfig = {
    {
        "database",
        // … (1 more, −62 tokens)
    },
    // … (3 more, −149 tokens)
};
```

**Стало (после CppInitializerListProcessor)**:
```cpp
std::map<std::string, std::map<std::string, int>> largeConfig = {
    {"database", {
        {"port", 5432},
        {"timeout", 30},
        // … (5 more, −35 tokens)
    }},
    // … (3 more, −63 tokens)
};
```

**Диагноз**: C++ entries (`{"database", {...}}`) обрабатывались как коллекции, что ломало структуру.

**Решение**: `CppInitializerListProcessor` фильтрует entries в `process()`, возвращая `None`.

### Проблема №2: Неполная оптимизация глубоких структур (Java) ✅ РЕШЕНА

**Было (после удаления DFS)**:
```java
Map<String, Object> nestedData = Map.of(
    "level1", Map.of(
        "level2", Map.of(
            "level3", Map.ofEntries(
                Map.entry("data", List.of(
                    Map.of("id", 1, "name", "First", "active", true),
                    Map.of("id", 2, "name", "Second", "active", false),
                    Map.of("id", 3, "name", "Third", "active", true),
                    Map.of("id", 4, "name", "Fourth", "active", true),
                    Map.of("id", 5, "name", "Fifth", "active", false)
                ))
                // … (1 more, −59 tokens)
            )
        )
    )
);
```

**Стало (после унифицированной inside-out сортировки)**:
```java
Map<String, Object> nestedData = Map.of(
    "level1", Map.of(
        "level2", Map.of(
            "level3", Map.ofEntries(
                Map.entry("data", List.of(
                    Map.of("id", 1, "name", "First", "active", true)
                    // … (4 more, −77 tokens)
                ))
                // … (1 more, −59 tokens)
            )
        )
    )
);
```

**Диагноз**: Inside-out сортировка работала только внутри одного profile. `List.of` (depth=17) обрабатывался ПОСЛЕ `Map.ofEntries` (depth=13), хотя был глубже.

**Решение**: Унифицированная сортировка всех nodes от всех profiles по depth.

### Проблема №3: Потеря корневого литерала (Go)

**Было**:
```go
return &LiteralDataManager{
    smallConfig: map[string]interface{}{...},
    largeConfig: map[string]interface{}{...},
    supportedLanguages: []string{...},
    allowedExtensions: []string{...},
} // literal struct (−495 tokens)
```

**Стало**:
```go
return &LiteralDataManager{
    smallConfig: map[string]interface{}{...},
    largeConfig: map[string]interface{}{...},
    supportedLanguages: []string{...}, // literal slice (−82 tokens)
    allowedExtensions: []string{...},  // literal slice (−56 tokens)
}
// Нет комментария о корневом struct
```

**Наблюдение**:
- ❌ Корневой struct literal не распознается/не комментируется
- ✅ Вложенные элементы обрабатываются корректнее с точки зрения бюджета

---

## Гипотезы причин

### Гипотеза 1: Проблема в `add_replacement_composing_nested`

Возможно, метод `_apply_nested_edits_to_text` неправильно применяет edits к replacement тексту при сложной вложенности.

**Проверка**: Добавить логирование в `RangeEditor._apply_nested_edits_to_text`

### Гипотеза 2: Проблема в порядке обработки

Inside-out сортировка может быть неполной - некоторые вложенные структуры обрабатываются в неправильном порядке.

**Проверка**: Логировать порядок обработки узлов в pipeline

### Гипотеза 3: Проблема в форматировании после композиции

`CollectionFormatter` может неправильно форматировать результат после применения вложенных edits.

**Проверка**: Сравнить `trimmed_text` до и после композиции

### Гипотеза 4: Tree-sitter запросы не захватывают корневые структуры

Go composite literal для struct может не попадать в запросы профилей.

**Проверка**: Добавить логирование найденных nodes для Go struct literals

---

## План отладки

### Этап 1: Локализация проблемы (текущий этап)

1. **Выбрать простейший failing case**: C++ пример (явная порча структуры)
2. **Добавить временное логирование**:
   - В pipeline: порядок обработки nodes
   - В RangeEditor: какие edits композируются
   - В CollectionFormatter: что форматируется
3. **Написать временный тест** без Golden, который покажет проблему

### Этап 2: Исправление

1. Исправить выявленную проблему
2. Проверить на простом временном тесте
3. Запустить полный прогон `./scripts/test_adapters.sh literals all`

### Этап 3: Итерация

Повторять Этап 1-2 для каждой категории проблем

### Этап 4: Финальная верификация

1. Перегенерировать Golden: `./scripts/test_adapters.sh literals all true`
2. Сравнить через `git diff` - оценить улучшения/ухудшения
3. При необходимости - финальные доработки
4. Закоммитить финальное состояние

---

## Технические заметки

### Важные места для отладки

**RangeEditor**:
- `add_replacement_composing_nested()` - точка композиции
- `_apply_nested_edits_to_text()` - применение вложенных edits к replacement

**Pipeline**:
- `_process_profile()` - сортировка и обработка nodes
- `node_depth()` - расчет глубины узла

**CollectionFormatter**:
- `format()` - форматирование результата
- `_format_multiline()` / `_format_single_line()` - специфичное форматирование

### Временные отладочные техники

```python
# В продуктовом коде
import logging
logging.debug(f"Processing node at depth {depth}: {text[:50]}")

# Временный тест без Golden
def test_debug_cpp_nested_map():
    code = """
    std::map<std::string, std::map<std::string, int>> config = {
        {"db", {{"port", 5432}, {"timeout", 30}}}
    };
    """
    result = process_with_literals(code, max_tokens=20)
    print(result)  # Визуальная проверка
    assert "db" in result
```

---

## История обновлений

- **2025-12-13 19:00**: Создан файл, зафиксировано текущее состояние после удаления DFS
- **2025-12-13 19:00**: Добавлен анализ 16 падающих тестов, гипотезы и план отладки
- **2025-12-13 22:30**: ✅ Проблема №1 решена через создание `CppInitializerListProcessor`
  - Архитектурное решение: language-specific компонент вместо хака в pipeline
  - Python и C++ тесты проходят
  - Структура C++ nested maps сохраняется корректно
- **2025-12-13 23:30**: ✅ Проблема №2 решена через унифицированную inside-out сортировку
  - Рефакторинг pipeline: все nodes от всех profiles сортируются вместе по depth
  - Depth важнее порядка профилей в дескрипторе
  - Java вложенные литералы разных типов оптимизируются правильно
  - Универсальное решение для всех языков

---

## Следующие шаги

1. ✅ Создать файл памяти (этот файл)
2. ✅ Закоммитить текущее состояние (DFS удален, тесты падают)
3. ✅ Выбрать простейший failing case для отладки (C++ nested maps)
4. ✅ Добавить временное логирование (debug тесты)
5. ✅ Написать временный отладочный тест
6. ✅ Исправить первую проблему (CppInitializerListProcessor)
7. ✅ Закоммитить решение проблемы №1
8. ✅ Перейти к проблеме №2 (Java неполная оптимизация)
9. ✅ Исправить проблему №2 (унифицированная inside-out сортировка)
10. ✅ Закоммитить решение проблемы №2
11. 🔲 Перейти к проблеме №3 (Go потеря корневого литерала)
12. 🔲 Финальная верификация через Golden (перегенерация + git diff)
13. 🔲 Удалить временные debug тесты
