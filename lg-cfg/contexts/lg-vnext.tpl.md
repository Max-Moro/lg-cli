В параллельной подветке диалога мы уже разработали основной код для новой версии инструмента "LG vNext" в пакете `lg_vnext/`. Для экономии контекстного окна, транкейчу диалог и присылаю текущий код. Если реализация модуля не предоставлена, то она не отличается от оригинальной версии в старом пакете `lg/` (перенесена как есть).

${lg-vnext-src}

Вот набор актуализированных тестов:

${lg-vnext-tests-src}


Есть ли у нас тесты, которые проверяют, что система кеширования реально работает внутри `lg_vnext/engine.py` и не приводит к повторному вызову тяжелых расчетов или обработчиков? 