В параллельно ветке мы уже разработали основной код для новой версии инструмента "LG vNext" в пакете `lg_vnext/`, присылаю него код. Если реализация модуля не предоставлена, то она не отличается от оригинальной версии в старом пакете `lg/`.

${lg-vnext-src}

Сейчас мы занимаемся работой по актуализации тестов. Я тебе порционно буду присылать вариант старых тестов и мы обсуждаем, актуальны ли они для новой версии и имеет ли смысл их переписывать.

Вот для примера код тестов, который мы уже успели разработать для новой версии:

${lg-vnext-tests-src}

Пока что ничего делать не нужно. Жди очередную порцию тестов из старого пакета.
